import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-workbench-diagnostics-ui'
export const inject: string[] = []

/** The Host fiber is the inventory-visible owner of the browser diagnostics tab. */
export function apply(_ctx: Context): void {}
