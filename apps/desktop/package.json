{
  "name": "@dsh-workbench/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm run build && electron .",
    "test": "pnpm --filter @dsh-workbench/desktop-core build && vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@dsh-workbench/desktop-core": "workspace:*",
    "@dsh-workbench/runtime": "workspace:*"
  },
  "devDependencies": {
    "electron": "catalog:"
  }
}
